iNumber = 0;
alert(iNumber++, console.log(iNumber) + iNumber, console.log(iNumber) * 2);


iNumber = 0;
alert(iNumber++ + (iNumber * 2));

iNumber = 0;
alert(iNumber++ + iNumber);

iNumber = 0;
alert(iNumber++ + iNumber * 2 + console.log(iNumber), iNumber++, console.log(iNumber));

iNumber = 0;
alert(iNumber++ + iNumber * 2 + iNumber++);

iNumber = 0;
alert(iNumber++ + iNumber * 2 + ++iNumber);

var iNum1 = 0;
var iNum2 = 0;

while (iNum1 == iNum2) {
    iNum1 = parseInt(prompt("Enter number 1:"));
    iNum2 = parseInt(prompt("Enter number 2:"));

}